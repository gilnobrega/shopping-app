import 'package:flutter/cupertino.dart';

class ItemTilePrice extends StatelessWidget {
  const ItemTilePrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
